# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Single-file slide deck for a talk titled *"From idea to LOTUSim contribution — faster with AI agents"*. Served by GitHub Pages from `main` at the repo root on each push — no build, no CI, no dependencies.

## Run locally

```bash
python -m http.server 8000   # then open http://localhost:8000
```

Or open `index.html` directly in a browser.

## Visual check (before claiming a slide change works)

The deck is projected at **1920×1080** — verify there, in **both themes**, before saying a change is done:

1. **Serve it** (`python -m http.server 8000`): a `file://` URL is **blocked** in Playwright/Chromium.
2. **Deep-link a slide** via the hash, e.g. `localhost:8000/index.html#7`. ⚠️ Re-navigating to the **same URL doesn't reload** (hash-only change) — add a cache-buster: `?v=2#7`.
3. **Resize to 1920×1080**, screenshot, and check **both themes** (white `theme-ng` is the default; press `t` for the dark one).
4. For layout doubts (centering, overflow), **measure with `getBoundingClientRect()`** instead of eyeballing a scaled screenshot — e.g. the footer centering fix (`align-self: center`) was invisible to a glance but obvious from the box (width 893, anchored left at the 120px padding, not full-width).

## Architecture

Everything lives in `index.html` (~2560 lines): CSS in `<style>`, slides as `<section class="slide">`, navigation logic in the trailing `<script>` IIFE. The deck was **reworked for 1080p projection** (2026-06-24, commit `98cab43`): **13 → 12 slides**, bigger/lighter, kernel+OpenClaw merged. See *Current state (2026-06-24)*.

- Slides — each `<section class="slide">` under `#deck`. The first carries `is-active`; the script toggles that class on the current slide.
- Counter — `#counter` shows `NN / total` and the script reads `slides.length`, so adding/removing a `<section>` updates the count automatically. Don't hardcode totals elsewhere.
- Navigation — keyboard (`→`/`Space`/`PageDown`, `←`/`PageUp`, `Home`/`End`, `f` for fullscreen, `t` to toggle the theme — white ↔ dark) and click-to-advance on the deck (anchors/buttons/inputs excluded). URL hash `#N` deep-links to slide N.
- Dynamic helpers in the IIFE:
  - `.countup` elements animate from 0 to `data-target` (with optional `data-suffix`, `data-format="comma"`) when their slide activates.
  - `.cols-2` / `.cols-3` children with class `card` get a staggered `--card-delay` CSS variable (0.15s + 0.09s × index).
  - Theme — the **Naval Group white theme (`body.theme-ng`) is the DEFAULT** since 2026-06-24 (hardcoded `class="theme-ng"` on `<body>`); the `t` key toggles it off to the dark theme, and `?theme=ng` still force-applies it.
- Key visual components (plain CSS/markup, no JS):
  - Slide 3 (Disambiguation; was slide 2) — two `.card`s contrasting the two senses of "agent" (LOTUSim platform vs the talk's tool-using LLM), plus a centred takeaway line. (An earlier `.loopflow` Perceive→Decide→Act box was dropped 2026-06-02 as too-early; its CSS was removed.)
  - Slide 7 (Pipeline overview; was slide 9) `.relay` — five pixel-sprite **agents** (Cartographer/Architect/Companion/Writer/Reviewer + verbs maps/plans/builds/writes/ships) that light up in cascade as a `.baton` orb arcs across (`batonRelay` 6s; `agentLight`/`agentLightFinal` — the Reviewer stays lit at the end). Sprites are generated from `data-sprite` bitmaps by `spriteToSVG()` in the IIFE (inline `<svg>` of run-length `<rect>`s). **This is Cyril's original design, restored 2026-06-02** — he's attached to it ("j'y tiens"); a clean text-pipeline rewrite (582839f) and an avatars+loop-ring variant were **both rejected. Don't replace it.** Glows use `var(--glow)` so they're theme-aware (added 2026-06-02 — the original hardcoded orange, which clashed with the NG blue theme).
  - Cover Naval Group logo — inline SVG `.ng-logo` with `.lg-primary` (wordmark) + `.lg-secondary` (accent) paths; fills switch by theme (white/blue wordmark, red accent always). Geometry came from naval-group.com's `logo.svg` sprite — keep it inline (self-contained), don't re-add a base64 PNG.
- Design tokens — CSS custom properties under `:root` (`--bg`, `--ink`, `--accent` orange `#FF5A36`, `--glow` = accent as `r,g,b` for glow shadows, fonts `--display` Instrument Serif / `--mono` JetBrains Mono). Use these instead of hardcoding colors/fonts. The `body.theme-ng` block at the end of `<style>` remaps them to the Naval Group white theme (white `--bg`, deep-navy `#061835` ink, brand blue `#164194` accent, red `#ed051d` danger, `--glow` → blue).

## Conventions

- Keep the deck a single self-contained HTML file. No bundler, no framework, no external assets beyond Google Fonts.
- Commit style: **gitmoji** (`:tada:`, `:truck:`, `:memo:`…) — not Conventional Commits. See git log for examples.
- When adding a slide, mirror the structure of a nearby `<section class="slide">` and reuse existing typography classes (`eyebrow`, `title`, `sub`, `lead`, `dim`, `hl`, `cols-2`, `card`, `stack`, …) rather than introducing new CSS.

## Fact-checking — training data is stale on these topics

The talk references events from late 2025 / early-to-mid 2026 that moved fast and on which Claude's training data is **outdated**. Before editing factual claims, **always verify on the web** (`mgrep --web --answer "..."`) — do not trust internal knowledge. Specifically:

- **Linux kernel AI-assistance policy** — adopted in 2026 (`Documentation/process/coding-assistants.rst`, `Assisted-by:` trailer). The slide source-of-truth beats any prior assumption.
- **OpenClaw / Clawbot / Clawd** — Peter Steinberger's agent, renamed several times (Clawbot → … → OpenClaw); old names in newsletter/podcast titles are expected, not bugs. Its creator was **acqui-hired by OpenAI** (14–15 Feb 2026; the project now lives in a **foundation OpenAI supports** — TechCrunch/CNBC confirmed; this is OFF-slide since 2026-06-09, speaker-notes Q&A only, with the "neutral example?" answer: the governance artifacts predate the deal). Live stats (GitHub API, **2026-06-09**): **377,824** stars, **58,238** commits on main (count = `Link` header last-page trick on `/commits?per_page=1`; don't confuse with **78,990 forks**), **26 named maintainers** in `CONTRIBUTING.md` (the old "28+" was stale → slide 6 now says "one of ~25 human maintainers"; recount before the talk). The old "≈600 security advisories" figure is **no longer on any slide** — web sources disagree wildly (138–280+); don't cite it without re-checking. **Workflow:** ≈**100 agents in parallel** (Codex / GPT-5.5) that code, **review each other's PRs**, hunt security bugs, dedupe issues — Steinberger *alone* burned ≈**$1.3M of tokens in a single month (April 2026)**: 603B tokens, 7.6M requests, "fast-mode" pricing (≈$300k without — nuance lives in speaker notes), **funded by OpenAI as research**. Human guardrails per `CONTRIBUTING.md`: every external PR needs a **"real behavior proof"** section, 20-open-PR-per-author limit, review bots (Codex/ClawSweeper) comment, a human maintainer signs the merge; AI assistance explicitly welcome if disclosed. Sources: the-decoder.com, tomshardware.com, github.com/openclaw/openclaw. "Fastest-growing open-source project in GitHub history" is Cyril's framing, corroborated. Re-verify live stats before each talk.
- **Cyril's role** — ⚠️ Cyril is **a lead developer at Naval Group**, **NOT the lead developer / maintainer of LOTUSim** (corrected 2026-06-25; he approaches open-source LOTUSim as an external-ish contributor — which IS the talk's audience and thesis). Earlier passes wrongly baked "lead developer of LOTUSim" into the deck; do not reintroduce it. On-slide self-id = "Lead Developer, Naval Group"; the homework/close are framed as a contributor's push, **not** a maintainer's roadmap commitment.
- **LOTUSim** — official repo is `github.com/naval-group/LOTUSim` (confirmed by Cyril, 2026-06-01; an earlier `LOTUSRobotics/LOTUSim` note was a hallucination). Naval Group hosts the **LOTUSim technical conference**; the 2026 edition is on **2 July 2026** (written out in full on the cover since 2026-06-09 — `02/07/2026` reads as Feb 7 in US format).
- **Model versions** — latest Claude as of mid-2026 is **Opus 4.8** (`claude-opus-4-8`, released 28 May 2026). (No on-slide `Assisted-by:` model example remains in the 13-slide deck — slide 9's Ship line mentions `Assisted-by:` without a model.) Exception: slide 5 keeps `Claude:claude-3-opus` on purpose — that's the *literal example from the kernel doc*, not a staleness bug, so don't "upgrade" it (gloss in speaker notes: "the format is what matters, not the model version"). Slide 4's `Opus 4.5 / GPT-5.2` are also intentional: they mark the late-2025 capability break that made agentic coding viable (**GPT-5.2 is real, released 11 Dec 2025** — re-verified 2026-06-09 after an external reviewer wrongly flagged it; GPT-5.5 is Apr 2026 and would be anachronistic there).
- **On-slide quotes** — verified 2026-06-01. The Torvalds *"The documentation is for good actors."* quote was **removed from slide 5** (2026-06-02): source fragile + his real tone was skeptical (Phoronix: *"the AI slop issue is NOT going to be solved with documentation"*). It was replaced by a non-attributed takeaway (*"the most conservative community in open source wrote the rules — rather than ban it"*). Steinberger's *"taste and system design remain the ultimate moats"* and *"close the loop…"* are corroborated. ⚠️ Gotcha: `mgrep --web` is **circular** for on-slide quotes — it matches our own `index.html` and reports it as a "source". Use the **`WebSearch`** tool for genuine external verification of anything already written in the deck.

## Current state & open items (2026-06-26)

**Unity "wow" vertical — UNPARKED + VALIDATED end-to-end on macOS (2026-06-26).** WSL was blocked by a structural wall (**HDRP renders in Vulkan; WSLg exposes no GPU Vulkan device** → Unity falls back to OpenGLCore, which HDRP rejects; only fixable via Dozen — not worth it). **The macOS pivot solves it**: all ROS/gz/xdyn runs in an **amd64 Docker container under Rosetta** (headless, no GPU — the M2's Metal is used only by native Unity), bridged to **native Unity on Metal** by `ros_tcp_endpoint` (ROS2). **Proven 2026-06-26**: a vessel driven by the emulated co-sim moves live on Unity's HDRP sea. Full runbook + every trap (symptom→cause→fix) in **`docs/macos-demo.md`** (the macOS gotchas are also condensed into the `lotusim-developer` skill, §6 of `render-bridge-and-coordinates.md`). **3 real upstream bugs found+fixed** (PR candidates): (1) the double-`Start` in `LotusimConnector.cs` (interface started twice — empty namespace then the real one), (2) `FASTDDS_BUILTIN_TRANSPORTS=UDPv4` disabling SHM → intra-container DDS discovery (subscriber-before-publisher) breaks → use `DEFAULT`, (3) ⭐ the **`GameManager` scene-redirect** — playing `defenseScenario` standalone triggers `if(!PhotonNetwork.IsConnected) LoadScene("Launcher")`, which reloads the scene and **destroys the `LotusimConnector`** (the render receiver) → nothing renders, **masquerading as a bridge/relay bug** (hardest to find). ⚠️ **Always bypass the Photon/Launcher login before a run.** Remaining = polish for the real capture: a `focus_v2` Addressable prefab (we validated with a `wamv` stand-in), the crab/+90° orientation, porting `ctrl_course.py` to round the buoy, and ideally a **minimal dedicated scene** (sea + connector + camera, no Photon) which avoids the redirect trap by construction. **Install Blender on the Mac** to build the Focus V2 mesh for the real run. The WSL attempt + verdict stay in `docs/unity-vertical.md`; the deck still ships with the validated gz-GUI sailboat clip as the safe demo.

**Contribution review done — `docs/contribution-review.md`.** Verified our LOTUSim work is by-the-rules and PR-readiness of each piece. Key: the **quaternion j/k bug** we hit (a steered vessel can't accumulate yaw / dead-reckons straight, invisible at identity) is an **UPSTREAM bug** (`xdyn_websocket.cpp`, introduced by naval-group's "Release November 2025"), a pure parsing bug (named `qj`/`qk` in wrong slots), **convention-neutral and does NOT regress Unity** (the gz↔Unity `Z→-Y` transform lives independently on the Unity side, in `common.cs`) → **PR-safe** (fix `8256296` + regression test `5849ab9`). `<control_surfaces>` (refactors shared cmd code — verify thrusters-only path) and the Unity double-Start fix (in prod code, not validated end-to-end) need a check before PR. Our `focus_v2.yaml` is wiki-compliant (rotation convention, body-frame-relative-to-mesh, full inertia, control surfaces via `hydrodynamic polar`).

**macOS / container side-finding.** LOTUSim already ships a **containerized image** (`ghcr.io/naval-group/lotusim`, root `dockerfile` param by `ROS_DISTRO`, CI builds jazzy+humble, release pushes to GHCR). Since ROS2 dropped native macOS support (Tier 3, unmaintained), the clean Mac path = **arm64 rebuild of that image + Unity native on Metal**, bridged by `ros_tcp_endpoint` (the same Unity-is-a-network-client split that bit us in WSL). A container built from **our fork** (`docker build --build-arg ROS_DISTRO=jazzy .` on `demo/focus-v2`) could also be a cleaner/reproducible **demo env**. Cyril moving to a Mac to explore this.

**Skill `lotusim-developer` extended** (versioned in `claude-config`, so it travels cross-machine): 2 new references — `ecosystem-and-architecture.md` (wiki index, `LOTUSim-generic-scenario` repo, Unity repos, fork) and `render-bridge-and-coordinates.md` (gz↔xdyn↔Unity data flow, coordinate conventions, render bridge, the WSLg HDRP wall, mesh gotcha). The WSL GPU note in `SKILL.md` was corrected (d3d12 = OpenGL/gz GUI only; Unity HDRP needs GPU Vulkan).

**Open items:** record the gz demo clip (still the deliverable, ~27 June; optionally from a fork-built container for reproducibility); the Mac/container track is separate/optional; the quaternion-fix issue/PR is ready to draft when Cyril wants. ⚠️ The native project memory (`~/.claude/projects/.../memory/`) is WSL-machine-local and mostly WSL-specific — the durable knowledge lives in this `CLAUDE.md` + `docs/` + the skill (all git-backed → travel to the Mac).

## Previous pass (2026-06-25)

**Sonar → sailboat pivot (this pass, not yet committed).** The hypothetical *sonar sensor* example was replaced everywhere by the **real thing we built**: an **RC sailboat (Focus V2, the SWARMz race boat) that rounds a buoy** in LOTUSim/Gazebo (see `docs/demo-sailboat.md`). Decisions taken with Cyril (3 forks):
- **Full pivot** of the scenario across slides **4** (terminal: `"…add an RC sailboat to LOTUSim"`), **6** (scenario: *Adding an RC sailboat that rounds a race buoy*; whole-stack re-mapped to physics/systems/assets/worlds/docs; footer *"hydro/aero model stays the naval engineer's"*), **8** (title *"The loop, on the sailboat"* + terminal = `colcon … physics_engine_interface` / `lotusim run … focus_v2_demo.world` / `ctrl_course.py → rounds the mark · closest 1.10 m`), and **9** (demo lead nods to *a sailboat rounding a buoy*). All verified in Playwright at 1920×1080, **both themes, no overflow, no JS errors** (measured via `getBoundingClientRect`, not eyeballed).
- **The engine bug is ORAL-ONLY** (Cyril's call) — NOT on any slide. The quaternion j/k swap found+fixed+tested during the sailboat work (fork `cmoron/LOTUSim`, branch `demo/focus-v2`) is the best oral illustration of *map* + *the loop*; it lives in `speech.md §8` (optional, first-to-cut), `speaker-notes.md §8` + a dedicated Q&A entry. On-slide, slide 6/8 mention only the *control-surfaces* engine **feature**, never the bug.
- **"Software wall ≠ domain wall" lesson on slide 10** — the old *"Not a substitute"* card was sharpened: AI drops the **software** wall in hours (install, launch, first feature, first bugfix); the **domain** wall (environment + vessel physics) stays and needs naval expertise. Reinforces the slide-6 honesty footer.
- **`speech.md` + `speaker-notes.md` resynced 13 → 12 slides** (they were a deck behind): the separate Linux (§5) + OpenClaw (§6) speaking sections were **merged into one §5 "Industry — two answers"** (~2:30, two columns), everything renumbered down by one, budgets + the appendix timing-check updated. This was a pre-existing desync, fixed in this pass.
- **`docs/demo-sailboat.md` reframed**: the **sailboat is the contribution-demo** (the spike was *preparatory*; the real video will be **re-recorded from scratch** on the unmodified repo); the bug is demoted from "the headline contribution" to "encountered in route, oral illustration of map+loop"; added the 3 talk lessons (software wall falls / domain gap remains / map+loop on the bugfix).
- Committed + pushed as `12ceafb` `:lipstick: pivot the deck's running example: sonar → real Focus V2 sailboat`.

**Role correction — Cyril is NOT the LOTUSim lead dev (2026-06-25, committed separately).** Cyril clarified he is **a lead developer at Naval Group**, not the LOTUSim maintainer (see Fact-checking). The deck's *"I am the lead developer of LOTUSim"* was a long-standing overclaim. Fixed: cover self-id → **"Lead Developer, Naval Group"**; `speech.md §1` → *"a lead developer at Naval Group"*; the close-slide commitment (`speech.md §12` + `speaker-notes.md §11/§12`) reframed from *"my commitment as lead dev / I will do that work"* to **what he'll push for and contribute as a Naval Group developer** (a contribution + a call, not a unilateral roadmap). The on-slide close already said *"…make LOTUSim … together"* (collaborative — kept). ⚠️ Cyril was AFK when this landed (he said "corrige ça" without giving his exact title) — **confirm the wording on his return** (esp. whether "Lead Developer, Naval Group" is the title he wants).

**1080p rework + footer/theme polish (commits `98cab43` → `3946c61` → `9b5ab13`).** The deck is now **12 slides** (was 13): the separate **Linux kernel (old 5)** and **OpenClaw (old 6)** slides were merged into ONE **"Industry · two answers"** slide (5), kernel + OpenClaw side by side. Reworked bigger/lighter for 1080p projection. Authoritative current map (HTML `<!-- N. -->` comments match):

1 Cover · 2 The wall · 3 Disambiguation (two senses of "agent") · 4 Why now (closes on **the question**) · 5 **Industry · two answers** (kernel + OpenClaw) · 6 Scenario (**sailboat + buoy**, was sonar) · 7 Pipeline overview (the relay sprites) · 8 The loop in action (**on the sailboat**) · 9 Demo · 10 Limits / honesty · 11 LOTUSim's homework · 12 Close.

Changes this pass (all verified in Playwright at 1920×1080, both themes, no JS errors):
- **Naval Group white theme is now the DEFAULT** (`<body class="theme-ng">`). `t` still toggles to the dark theme; `?theme=ng` still force-applies white.
- **Git rescue:** an `autostash` conflict (a stale 2026-06-09 WIP reapplied onto the 1080p rework) was resolved by **keeping the rework (HEAD)**; the autostash had **dropped 4 already-decided 06-09 items**, re-grafted onto the new structure: cover **`2 July 2026`**, slide-9 demo line **"Recorded on the repo as it is today: no AGENTS.md, no prepared context"**, slide-11 homework **`SECURITY.md`** bullet, slide-8 **"Local CI first … (remote CI still gates)"**. (The verbose 06-09 stats/maintainer wording was intentionally cut by the kernel+OpenClaw merge — superseded, not lost. The autostash `stash@{0}` was kept as a safety net; drop it once happy.)
- **Slimming (commit `3946c61`):** slides 4 / 8 / 11 cut to **telegraphic bullets** (dropped the repeated "the agent reads / it proposes / it runs…" subjects; **terminal mocks left intact**). The slide-4 driving question and the relay footer kept verbatim.
- **Footers homogenised + centered (commit `9b5ab13`):** ALL footers now use **one** `.footer-note` style — 24px, `--ink`, **centered**, no uppercase, no "→" prefix (kept only on the terminal-mock line `→ targeted patch`). The old small/dim/uppercase default and the `.lead` modifier are **gone**. ⚠️ **Centering needs `align-self: center`** — `text-align:center` alone does NOT center, because `.slide` is `flex-direction:column` and the `<p>` shrinks to its content width (anchored left at the 120px padding). `max-width: 70ch` keeps long footers to a readable measure.
- **Slide 5 de-listed:** the kernel + OpenClaw answer points moved from `ul.checks` (a "›" bullet glued onto a full sentence = unreadable) to **`div.answer-points` of `<p>`** (readable paragraphs, light dashed separators). New CSS `.answer-points`. The deck's other `ul.checks` lists (slides 6, 8, 11, 12) are untouched.

**Open items:**
- **Slide 9 demo video** — still to record (sped-up, on the **unmodified** LOTUSim repo, by ~27 June). ⚠️ The placeholder footer "Drop the sped-up screen capture here…" is now a **prominent centered line** — replace it with the actual video before the talk so it doesn't show on stage.
- **Stats refresh J-1** — re-sample OpenClaw stars/commits + recount maintainers before 2 July (the merged slide 5 shows `376k ★` as a tag; the old countup stats were dropped).
- ⚠️ **The 2026-06-07 pass below uses the OLD 13-slide numbering** (slide 9 relay, 10 demo, 12 homework, 13 close…) and predates the kernel/OpenClaw condensation (some literal examples may have been trimmed). The map above is authoritative — verify against current `index.html` before trusting a slide number there.

---

## Previous pass (2026-06-09)

**Review pass #3 (2026-06-09, multi-agent: AI-agents expert / TEDx / open-source / grill-me critic, adjudicating an external review Cyril brought in).** Verdict on the external review: right on stale stats, the "28+ humans sign the merge" phrasing, the demo/homework tension and the cover date; **wrong on GPT-5.2** (real, 11 Dec 2025 — the late-2025 anchor stands) and on the slide-8/9 "tension" (the "one agent, five hats" footer was already on slide 8); over-corrective on putting fast-mode pricing, an Opus-4.8 line, and "local/on-prem" on the slides (all routed to speaker notes instead). Changes applied (all verified in Playwright, 13 slides, no JS errors):
- Cover: `02/07/2026` → **`2 July 2026`**.
- Slide 6: stats → **377k / 58,238** (sampled 2026-06-09); "28+ humans sign the merge" → **"one of ~25 human maintainers signs the merge"** (26 named in CONTRIBUTING.md, counted directly); "built agent-first, end to end" → **"developed with agents at the centre of the workflow from day one"** (the product is a personal assistant; the *dev workflow* is what's agent-first); dim paragraph condensed to one line ("The scale … is not the lesson. The lesson is the discipline.") — "hired by OpenAI" moved off-slide (acqui-hire nuance in Q&A notes).
- Slide 9 footer: "Local CI > remote CI" → **"Local CI first … (remote CI still gates the merge)"** (don't antagonise the DevOps in the room).
- Slide 10: **new line "Recorded on the repo as it is today: no AGENTS.md, no prepared context."** — Cyril DECIDED (2026-06-09) the demo video will be recorded on the **unmodified** LOTUSim repo; this is now claimed on-slide as the credibility beat, and answers the "was the demo prepped?" trap. ⚠️ The video MUST actually be recorded that way.
- Slide 12: **`SECURITY.md` added** to the homework list (the open-source agent's strongest point: OpenClaw's 150+ CVEs show governance must precede scale; extra-relevant for a defence audience).
- Speaker notes: fast-mode ($300k) nuance, acqui-hire context + "neutral example?" Q&A, OpenClaw-CVEs Q&A, "does the agent install my ROS/Gazebo env?" Q&A, raw-repo demo verbatim + PREP CHECK (record by ~27 June), slide 6→7 bridge line, "moats" ESL gloss, claude-3-opus gloss.
- Known leftovers deliberately NOT done: slide-13 "The moment" trim and slide-8 subtitle tweak (TEDx-agent solo suggestions, no consensus); "fastest-growing" kept as-is (corroborated; Q&A-ready).

---

## Previous pass (2026-06-07)

**Major restructure (2026-06-07, multi-agent review + brainstorm): deck went 18 → 13 slides.** Thesis changed from a *survey* ("the industry legitimised AI agents") to a **concrete two-sided answer**: how to accelerate LOTUSim with agents = (1) the **contributor** adopts the agentic loop + responsibility (`Assisted-by`, the human signs), and (2) the **project/governance** makes LOTUSim *agent-ready* by writing the implicit down. Driving line: ***"an agent is only as good as the docs it can read."*** The 13 slides (HTML `<!-- N. -->` comments match):

1 Cover · 2 **The wall** (hook — implicit conventions, sets up the payoff) · 3 Two kinds of "agent" (mini, ~45s) · 4 Why now (closes on **the question**: *how do we handle agent-assisted contributions?* → bridges into Linux/OpenClaw as *answers*) · 5 Linux kernel · 6 OpenClaw · 7 Scenario (sonar) · 8 Pipeline relay · 9 The loop in action · 10 Demo · 11 Limits · 12 LOTUSim's homework · 13 Close.

**Narrative arc (Arc A, chosen 2026-06-07 after a 2nd brainstorm; audience = mix incl. newcomers):** open on the **wall** (shared pain), then mini-definition, then *Why now* closing on **the question** ("how do we handle agent-assisted contributions, responsibly?") so the industry block reads as *answers*, not examples. Industry proof (Linux+OpenClaw) stays **before** the LOTUSim sonar block. **Arc A IS the TEDx-agent's recommended arc** (Problem → Proof → Method → Contract). The *alternative* (industry **after** the demo, bridging into the homework) came from the orchestrator's synthesis + the audience-proxy/grill-me agents — it was **not** chosen. One clean general→LOTUSim pivot after slide 6. **Oral (not on slide):** at slide 4, drop the local cautionary case — the ~13 PRs / ~300k lines that hit LOTUSim and weren't merged (no frame; some content that shouldn't have been public) — un-named; motivates the question.

Key changes this pass:
- **Slides 5 & 6 reframed around the real artifacts** (verified by fetching the repos): kernel `coding-assistants.rst` is *addressed to agents and their users*; the safe takeaway is *"yes, under one condition: the human who signs answers for every line"* (the doc literally says **"AI agents MUST NOT add Signed-off-by"**). Slide 5 names **"the Linux kernel"** explicitly (not just "kernel"). OpenClaw leads with *what they wrote down* as clean filename-led bullets: `VISION.md` (what to build / what to refuse) + `AGENTS.md` (how to build & test in-repo). Right-column numbers = **376k stars + 56,098 commits** (the 35 KB AGENTS.md figure was tried and dropped — low value). ⚠️ **`~100 agents` + `$1.3M` are attributed to Steinberger PERSONALLY, not "the project"** (don't conflate — earlier draft did).
- **Em-dashes swept** from all visible slide text (Cyril's note: too many "—", not human). Replaced by commas/colons/periods. Kept only in CSS/JS comments, the `<title>`, and citation attributions (`— P. Steinberger`).
- **Old "Distilled principles" slide cut** (folded into 5/6 + the loop).
- **5 stage slides (MAP/PLAN/BUILD/DOC/SHIP) collapsed into ONE** slide 9 *"The loop, on the sonar"* — Cyril felt he had little to say per stage, and the **video demo is the detailed how**. Keeps two human accents: *taste* at Plan, *close-the-loop* at Build.
- **Slide 7 (sonar) honesty fix:** the agent writes the *structure* (plugin/message/zero-stub); the **acoustic model stays the engineer's**, validated before merge. (Don't claim AI replaces acousticians — they don't code anyway; their knowledge is an input, or mocked by the contributor.)
- **Slide 8 relay** kept (Cyril's sprites — untouched), footer now *"one agent, five hats — not a hundred in parallel"* (anti-FOMO; he wants to detension the parallel-agent-army hype).
- **Slide 10 demo:** the invented **≈3h timeline was removed** (Cyril: "sorti du chapeau", bothered him). Now a placeholder for a **sped-up video** (he'll record it), no fabricated numbers.
- **Slide 11 limits:** confidentiality card → *"Govern what the agent touches"* — carries an **allusion to the ~300k-line ungoverned AI PRs** that hit LOTUSim (closed unmerged; some data shouldn't have been public). Deliberately *not* named on slide; everyone gets it at half-word.
- **Slide 12 NEW "LOTUSim's homework"** (replaces old "What it changes"): a *today vs agent-ready* gap (README 1 KB / docs in a wiki / nothing for agents → AGENTS.md, in-repo VISION, docs-as-code, test suite + local CI, SECURITY.md (added 2026-06-09), AI policy). The **governance contract**, carried by Cyril as lead dev.
- **Slide 13 close:** two-column CTA — *If you contribute* / *What the project will do (my commitment)* + signature.
- Dead CSS removed (`.bignum`, `.reveal-num`, `.endgrid` — the features that used them are gone).
- Verified rendering via Playwright (13 slides, no JS errors). Speaker notes rewritten as an ESL near-script with per-slide **Say:** verbatim lines + timing budget.

Earlier baseline (commit `582839f` + logo follow-up): Naval Group white theme (`t` / `?theme=ng`), real inline NG vector cover logo.

**LOTUSim repo doc reality (inspected 2026-06-07, doc only):** `README.md` ~1 KB, decent human `CONTRIBUTING.md` (issue→label→fork→PR→review + governance committee, biannual roadmap), but **no `AGENTS.md`/`CLAUDE.md`/`VISION.md` in-repo, docs in a GitHub wiki, `docs/` holds only images, testing guide "available soon".** This gap *is* the slide-12 message.

**Audience:** talk given at the LOTUSim technical conference (Naval Group — defence/naval; LOTUSim is a multi-agent *simulator*, MAS, not AI agents). **Most attendees are external partners who will contribute to the open-sourced LOTUSim** — keep jargon low; do **not** scare them into thinking they must self-host local models (the event is about the open-source part). Slides are **English**; chat with Cyril in **French** (his preference).

**Preview:** open `index.html`, navigate `←/→`, toggle the Naval Group white theme with **`t`** (or `?theme=ng`).

**Open items:**
- **Slide 8 relay** — Cyril may still want to *add* a loop illustration on top of the sprites ("on itérera dessus pour illustrer la loop") — the relay itself stays.
- **Slide 10 demo** — sped-up **video to be recorded** by Cyril **on the unmodified repo** (now claimed on-slide), by ~27 June; remove the `[...]` brackets from the slide-10 footer once the video is in.
- **Stats refresh J-1** — re-sample OpenClaw stars/commits + recount maintainers just before 2 July (slide 6 `data-target`s + `sampled` date).
- **Slide 12 homework = a real intent** — Cyril intends to actually **propose/contribute** `AGENTS.md` / in-repo `VISION` / an AI-contribution policy to LOTUSim (as a Naval Group contributor, pushing for it — NOT as the maintainer; see the role correction in Fact-checking). Track as contribution work, not just a slide.
- **Scenario-generation use case** (business/*métier* agents, not dev agents) is deliberately out of scope — one oral nod on the Close + a Q&A answer in speaker-notes; not a slide.
- Not yet committed — propose a gitmoji commit when Cyril is happy.
